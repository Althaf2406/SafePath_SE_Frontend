import SwiftUI
import Combine

/// Person 3: Offline map management screen.
struct OfflineMapView: View {
    // TODO: Person 3 will implement offline map download, display, and management.
    // TODO: Person 3 will use Person 1's shelter coordinates to pre-download map regions.
    var body: some View {
        Text("Offline Maps — Person 3 will implement")
            .foregroundColor(.secondary)
    }
}
